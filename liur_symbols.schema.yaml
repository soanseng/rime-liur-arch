# Rime schema
# encoding: utf-8

schema:
  schema_id: liur_symbols
  name: "〔依賴方案〕符號表"
  version: "1.0"
  description: |
    供嘸蝦米符號清單（`）掛載；非獨立輸入方案

switches:
  - name: ascii_mode
    reset: 0

engine:
  processors:
    - ascii_composer
    - key_binder
    - speller
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - abc_segmentor
    - fallback_segmentor
  translators:
    - table_translator
  filters:
    - uniquifier

speller:
  alphabet: "abcdefghijklmnopqrstuvwxyz"

translator:
  dictionary: liur_symbols
  enable_completion: true
  enable_sentence: false
  enable_user_dict: false

key_binder:
  import_preset: default
